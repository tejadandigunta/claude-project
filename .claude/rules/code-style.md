# Code style

- Type-hint all function signatures.
- Prefer stdlib over adding a dependency for something this small.
- Keep functions under ~20 lines; split when a function does two things.
- No comments explaining *what* code does — only *why*, when it's non-obvious.
