# Git workflow

- Small, focused commits; imperative mood subject line ("add", not "added").
- Never commit unless explicitly asked.
- Never force-push or rewrite history without explicit confirmation.
