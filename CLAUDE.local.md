# Local notes (not committed)

Personal scratch space for this machine — API keys, local paths, throwaway TODOs.
This file is gitignored; put things here you don't want in the shared CLAUDE.md.

Example:
- Local venv: `.venv/`
- I like `-n 10` instead of the default `-n 5` when running the CLI locally.
